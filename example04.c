#include <stdio.h>
#include <math.h>
#include <string.h>

int main()
{
    char seq[3000];
    int totcount=0;

    while (scanf("%s", seq) == 1)
    {
        totcount++;
    
    }
    printf("The totalseq is %d \n", totcount);
    
}